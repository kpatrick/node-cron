{
  "name": "node-cron-no-spam",
  "version": "2.0.3-no-spam",
  "description": "A simple cron-like task scheduler for Node.js based on node-cron v2.0.3 but without opencollective-postinstall",
  "author": "Lucas Merencia",
  "license": "ISC",
  "homepage": "https://github.com/kpatrick/node-cron",
  "main": "src/node-cron.js",
  "scripts": {
    "test": "nyc --reporter=html --reporter=text mocha --recursive",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "check": "npm test && npm run coverage"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kpatrick/node-cron.git"
  },
  "keywords": [
    "cron",
    "scheduler",
    "schedule",
    "task",
    "job"
  ],
  "bugs": {
    "url": "https://github.com/kpatrick/node-cron/issues"
  },
  "devDependencies": {
    "coveralls": "^3.0.2",
    "expect.js": "^0.3.1",
    "istanbul": "^0.4.2",
    "mocha": "^4.0.1",
    "nyc": "^13.0.1",
    "sinon": "^4.1.2"
  },
  "dependencies": {
    "tz-offset": "0.0.1"
  }
}
